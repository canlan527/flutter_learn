void main(List<String> args) {
  // ?? 当变量值为null时，会赋值给变量
  // 1. ??=
  // var name = null;
  // name ??= 'why';
  // print(name);

  // 2. ?? 如果 name 值为 null，就把后面的值付给temp
  // var name = null;
  // var temp = name ?? 'jxz';
  // print(temp);

}