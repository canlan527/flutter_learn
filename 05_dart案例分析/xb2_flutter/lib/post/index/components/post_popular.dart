import 'package:flutter/material.dart';

class PostPopular extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.local_fire_department_outlined,
      size: 128,
      color: Colors.black12,
    );
  }
}
